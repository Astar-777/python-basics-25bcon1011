# README audit table

This is the core deliverable.

| Claim made in README | True? | Evidence or correction made |
|---|---|---|
| Requires `pip install -r requirements.txt` | No | No `requirements.txt` exists, and the programs use only Python's built-in functionality. Delete this requirement if present. |
| No external libraries required | Yes | The programs use basic Python features and the `dataclasses` module, which is part of Python's standard library. No third-party packages are required. |
| Project structure includes `README.md` | No | There is currently no `README.md` file in the project structure. The README should not claim that it already exists. |
| Project is licensed under the MIT License | No | No license file currently exists in the project. The MIT License claim and `LICENSE` file reference should be removed unless a license is actually added. |
| Student program uses a `Student` class | Yes | The student program defines a `Student` class and stores the student's name, roll number, and marks as attributes. |
