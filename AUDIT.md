| Commit | My message | AI message | Which is clearer, and why? |
|---|---|---|---|
| 1 | upload factorial.py | upload factorial.py | Both are equally clear because the message directly states that `factorial.py` was uploaded. |
| 2 | upload fibonacci and struct program | upload fibonacci and struct program | Both are clear because the message directly states that `fibonacci` and `struct` program was uploaded. |
| 3 | updated factorial.py to not use math module | refactor: replace math factorial with loop | The AI message is clearer because it briefly describes the actual code change: replacing the `math.factorial()` function with an iterative loop. |
