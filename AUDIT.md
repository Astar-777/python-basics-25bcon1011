| Commit | My message | AI message | Which is clearer, and why? |
|---|---|---|---|
| 1 | upload factorial.py | upload factorial.py | Both are equally clear because the message directly states that `factorial.py` was uploaded. |
| 2 | upload fibonacci and struct program | upload fibonacci and struct program | Both are clear because the message directly states that `fibonacci` and `struct` program was uploaded. |
| 3 | updated factorial.py to not use math module | refactor: replace math factorial with loop | The AI message is clearer because it briefly describes the actual code change: replacing the `math.factorial()` function with an iterative loop. |


## Peer Repository Review

**Name:** Yagyadeep  
**Roll Number:** 25BCON0995  

### 1. Understand in 15 Seconds

The repository contains three simple Python programs: `factorial.py`, `fibonacci.py`, and `struct.py`.  
The programs demonstrate basic programming concepts such as loops, variables, calculations, and classes.

### 2. Verify Two Claims

**Claim 1:** No external dependencies are required.  
**Verified:** Yes. The programs use Python's built-in functionality and standard library only.

**Claim 2:** The student program stores name, roll number, and marks.  
**Verified:** Yes. The `Student` class contains attributes for the student's name, roll number, and marks.

### 3. Read Commit Messages

The commit message clearly describes the addition of Python programs and the concepts demonstrated. However, it originally included some claims that did not match the actual repository, such as the presence of a `README.md` and an MIT license.

### 4. One Specific Fix

The Fibonacci program could be improved by adding a short comment explaining the purpose of the variables `a` and `b`. This would make the logic easier for a beginner to understand without changing the program's functionality.

### Peer Review Summary

The repository is simple and easy to understand. The programs work as expected, and the README was improved by removing inaccurate claims about the license and project structure.
